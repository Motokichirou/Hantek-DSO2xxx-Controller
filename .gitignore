# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Archives / installers (не коммитим бинарные дистрибутивы — напр. NI Package Manager)
*.zip
*.exe

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Captured data / local outputs (keep the repo clean of measurement dumps)
captures/
*.npy
*.npz
*.hdf5
*.h5
!docs/**/*.png

# SCPI-логи (пишутся кнопкой LOG в корень проекта; не трекаем)
scpi_*.log

# Локальные выгрузки приложения по умолчанию (Save/Screenshot/Presets)
/waveform.csv
/waveform.npz
/waveform.h5
/scope.png
/preset.json

# Машинно-зависимый ярлык запуска (абсолютные пути)
*.lnk

# Дампы прошивки/калибровки прибора (локальная песочница, серийник+калибровка — не в репо)
tools/
